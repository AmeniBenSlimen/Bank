package com.pfe.Bank.repository;

import com.pfe.Bank.model.SVEnum;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VEnumRepository extends JpaRepository<SVEnum, Long> {
}
