package com.pfe.Bank.repository;

import com.pfe.Bank.model.SVInterval;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SVIntervalRepository extends JpaRepository<SVInterval, Long> {
}
